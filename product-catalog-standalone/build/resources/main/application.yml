server:
  port: 8282

management:
  endpoints:
    web:
      exposure:
        include: "*"
spring:
  h2:
    console:
      enabled: true
      path: "/h2"
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
