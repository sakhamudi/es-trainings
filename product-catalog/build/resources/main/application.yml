server:
  port: 8282


management:
  endpoint:
    refresh:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
spring:
  datasource:
    url: jdbc:h2:tcp://localhost:9092/mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
